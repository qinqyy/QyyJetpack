<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.qyy.mydatabinding">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".VsActivity" />
        <activity android:name=".RecyActivity" />
        <activity android:name=".LifecyclesActivity" />
        <activity android:name=".livedata.LiveDataActivity" />
        <activity android:name=".livedata.transformation.MapActivity" />
        <activity android:name=".livedata.transformation.SwitchMapActivity" />
        <activity android:name=".livedata.mediator.MediatorActivity" />
        <activity android:name=".viewmodel.ViewModelActivity" />
        <activity android:name=".room.RoomActivity" />
        <activity android:name=".navigation.NavActivity"/>
        <activity
            android:name=".navigation.NaviBottomActivity"
            android:label="@string/title_activity_navi_bottom"/>
        <activity android:name=".paging.PagingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>